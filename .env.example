# Database Configuration
# Railway provides MYSQL_URL automatically when you add a MySQL service
# MYSQL_URL=mysql://user:password@host:port/database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=task_nexus
DB_PORT=3306

# Server
PORT=5000

# CORS - comma-separated allowed origins (not needed if client is served from same origin)
# CLIENT_ORIGIN=https://your-app.up.railway.app

# JWT Authentication
JWT_SECRET=your_super_secret_jwt_key_change_this
